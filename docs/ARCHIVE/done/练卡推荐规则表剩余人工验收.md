# 练卡推荐规则表剩余人工验收

> 文档角色：archive
> 生命周期状态：completed
> 替代项：docs/练卡推荐规则.md；docs/TODO/练卡推荐规则来源精修.md
> 历史原因：三条 needs_review 已按外部体系原文裁决并清零
> 快照日期：2026-07-20
> 摘要：保存可露希尔与企鹅物流练卡规则的人工验收结论

## 验收结论

| rule id | 结论 | 依据 |
|---|---|---|
| `blackkey_closure` | 只保留可露希尔 E2 单核；第三散件不写入该规则 | vault「何时值得开」「搭配建议」+ `特别订单` 技能 |
| `penguin_exusiai_lemuen` | 能天使 E2「物流专家」+ 蕾缪安 E2「相伴」 | vault「路线 B：能蕾」「数值锚点」 |
| `penguin_texlap_e0` | 德克萨斯 E0「恩怨」+ 拉普兰德 E2「醉翁之意·β」 | vault「路线 A：德狼」「德克萨斯的技能说明」 |

三条规则只保留来源精确证明的核心成员，空、芬、黑键、吉星等排班候选不再借本规则产生培养建议。核心目标均已补技能名和具体 evidence heading，`review.status` 改为 `confirmed`，迁移期把排班匹配下限误当培养目标的 conflicts 已删除。当前 28 条规则全部为 `confirmed`。

## 验收证据

- 中文规则验收稿可由 `scripts/render_training_recommendations.py` 重建。
- `cargo test -p infra-core training_advice` 覆盖三条规则的目标练度、技能名和 confirmed 状态。
- `data/operbox_gongsun.json --answer` 的待复核分区为 0。

## 拆分开放项

已确认规则中仍有宽泛 evidence、缺 `benefit` 或缺技能名的条目，统一交由 `docs/TODO/练卡推荐规则来源精修.md`，不阻塞当前确定性推荐。
