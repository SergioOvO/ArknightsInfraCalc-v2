# 练卡推荐规则来源精修

> 文档角色：active-change
> 生命周期状态：ready-on-request
> 当前真源：docs/练卡推荐规则.md
> 摘要：逐步补齐已确认练卡规则的具体来源、收益标签和技能元数据

## 当前边界

`data/training_recommendations.json` 的 28 条规则均为 `confirmed`。本任务只提升来源与解释质量，不重新打开已确认的过滤语义。

## 后续工作

1. 为仍使用宽泛 evidence 的规则补具体 `heading`。
2. 只在来源明确时补 `benefit`、`skill_id` 和 `skill_name`，不机械生成。
3. 按 `gongsun-training-review` 逐条核对修改，不使用 `standalone_roster.json` 证明培养价值。
4. 规则语义若与 canonical 冲突，单独进入 `needs_review` 并请求裁决。
5. 单独核对 `penguin_texangel_e2` 与企鹅第三散件候选，未取得精确培养来源前不扩展已裁决的能蕾/德狼规则。

## 非目标

- 不修改 solver 候选池或 `data/standalone_roster.json`。
- 不改变当前目标练度、优先级、hard admission 或获取策略，除非另有明确裁决。
- 不实现前端、embedding 或自由生成模型。
