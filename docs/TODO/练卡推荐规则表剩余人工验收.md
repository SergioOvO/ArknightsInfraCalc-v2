# 练卡推荐规则表剩余人工验收

> 文档角色：active-change
> 生命周期状态：ready-on-request
> 当前真源：docs/练卡推荐规则.md
> 摘要：裁决剩余 needs_review 规则并补齐规则表来源精度

## 当前边界

`data/training_recommendations.json` 已完成 v2 schema、确定性过滤、N-of-M 核心准入和仓库内可证明规则精修。当前 28 条规则中 25 条为 `confirmed`，以下 3 条仍为 `needs_review`，不得作为确定推荐：

| rule id | 待裁决问题 | 所需来源 |
|---|---|---|
| `blackkey_closure` | 可露希尔特别订单单核语义与可露希尔/黑键/吉星同站组合的边界；`kind` 与成员角色 | 外部 vault「可露希尔特别订单」原文 + 当前组合口径 |
| `penguin_exusiai_lemuen` | 能天使目标取 E0 相伴门槛还是 E2 物流专家满档 | 外部 vault「企鹅物流」原文 + 用户培养口径 |
| `penguin_texlap_e0` | 拉普兰德目标取 E0 组合门槛还是 E2 醉翁之意·β | 外部 vault「企鹅物流」原文 + 用户培养口径 |

## 后续工作

1. 按 `gongsun-training-review` 生成最新中文验收稿。
2. 读取上述条目的具体外部来源段落，不从 `base_systems.json` 或 top hit 反推培养语义。
3. 对每条给出通过、修改或待裁决结论；获得裁决后同步规则、canonical 和边界回归。
4. 为仍使用宽泛 evidence 的已确认规则逐步补 `heading`；`benefit`、`skill_id` / `skill_name` 只在来源明确时补，不机械生成。
5. 全部 `needs_review` 清零且来源可追溯后关闭本任务。

## 非目标

- 不修改 `data/standalone_roster.json` 或 solver 候选池。
- 不引入设施、布局、生产目标或战斗培养过滤。
- 不实现 embedding、前端页面或自由生成模型。
