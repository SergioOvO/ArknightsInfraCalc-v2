# 体系注册表后续缺口

> 文档角色：active-change
> 生命周期状态：ready-on-request
> 当前真源：docs/ORCHESTRATION_LAYER.md；docs/BASE_ASSIGNMENT.md；docs/EFFECT_ATOM_DESIGN.md
> 摘要：承接旧组合体系规范化报告中仍有效但未获当前授权的开放项

旧审计完整上下文见 [组合体系规范化审计报告](../ARCHIVE/plans/SYSTEM_REGISTRY_NORMALIZATION_REPORT.md)。恢复本任务时必须先与 current owner 和当前代码对账，不能直接照搬旧阶段设计。

## 开放项

- 核对跨站 provider / consumer 与 global resource 的唯一 owner，删除仍存在的重复 registry 路径。
- 仅在 confirmed domain semantics 需要时，为制造同站 bond 建立具名机制；不得从旧报告自动恢复泛化方案。
- 建立体系激活、缺核心关闭、冲突、布局和最终落位的风险匹配回归矩阵。

## 非目标

- 不恢复旧 fixed room、priority 或 tag 来代替 required admission。
- 不把本文件作为业务真源；任何语义先更新对应 canonical。
