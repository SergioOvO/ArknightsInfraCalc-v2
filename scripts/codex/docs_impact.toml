schema_version = 2

# Only generated, local-only, editor, and raw-evidence paths may be excluded.
# Source-to-document ownership lives exclusively in Markdown lifecycle metadata.
ignore_globs = [
  ".git/**",
  ".vscode/**",
  "target/**",
  "out/**",
  "feedback/*/**",
  "data.zip",
  "从公孙长乐电脑里搜集到的资料/**",
]
